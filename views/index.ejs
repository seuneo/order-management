<!DOCTYPE html>
<html lang="en">

<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Order Scheduler</title>
  <link rel="stylesheet" href="/styles.css">
</head>

<body>
    <h1>Welcome to order scheduler</h1>
    <h2>Add Order</h2>
  <form action="/add" method="post">
    <div>
      <input type="text" name="name" placeholder="client name" autofocus autocomplete="off">
    </div>
    <div>
        <input type="text" name="contact" placeholder="contact" autofocus autocomplete="off">
      </div>
      <div>
        <input type="text" name="order" placeholder="order" autofocus autocomplete="off">
      </div>
    <button type="submit">Add</button>
  </form>

  <form action="/seeAllOrders" method="POST">
    <input type="submit" value="See Orders">
  </form>

  <div>
        <% if(locals.order){ %>
                <% for(let i = 0; i<order.length; i++){ %>
                    <p>
            <%=order[i].name%> <%=order[i].orders%> 
                    </p>

            <%}%>       
        <%}%>    
</div>
 
</body>

</html>